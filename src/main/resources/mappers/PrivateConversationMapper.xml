<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carbonpulse.mapper.PrivateConversationMapper">

    <!-- 根据两个用户ID查询私聊会话 -->
    <select id="selectByUsers" resultType="com.carbonpulse.entity.PrivateConversation">
        SELECT *
        FROM private_conversation
        WHERE (user_id_1 = #{a} AND user_id_2 = #{b})
           OR (user_id_1 = #{b} AND user_id_2 = #{a})
    </select>

    <!-- 清空用户1在该会话中的未读计数 -->
    <update id="clearUnreadForUser1">
        UPDATE private_conversation
        SET unread_count_user1 = 0
        WHERE id = #{conversationId}
          AND user_id_1 = #{userId}
    </update>

    <!-- 清空用户2在该会话中的未读计数 -->
    <update id="clearUnreadForUser2">
        UPDATE private_conversation
        SET unread_count_user2 = 0
        WHERE id = #{conversationId}
          AND user_id_2 = #{userId}
    </update>

</mapper>